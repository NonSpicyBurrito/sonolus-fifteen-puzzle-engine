{
  "name": "sonolus-fifteen-puzzle-engine",
  "version": "1.1.0",
  "description": "A Fifteen Puzzle engine in Sonolus.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:play": "sonolus-cli --dev ./play",
    "dev:tutorial": "sonolus-cli --dev ./tutorial",
    "prebuild": "tsc -p ./shared --noEmit && tsc -p ./play --noEmit && tsc -p ./tutorial --noEmit && eslint --ext .ts ./shared && eslint --ext .ts ./play && eslint --ext .ts ./tutorial && prettier . --check",
    "build": "sonolus-cli --build ./play && sonolus-cli --build ./tutorial"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.7.2",
    "@typescript-eslint/parser": "^6.7.2",
    "eslint": "^8.49.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.0.3",
    "prettier-plugin-organize-imports": "^3.2.3",
    "sonolus-core": "~7.3.0",
    "sonolus.js": "~9.2.0",
    "typescript": "~5.2.2"
  }
}
